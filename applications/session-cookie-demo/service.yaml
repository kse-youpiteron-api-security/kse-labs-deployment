apiVersion: v1
kind: Service
metadata:
  name: session-cookie-demo
  namespace: applications
  labels:
    app.kubernetes.io/name: session-cookie-demo
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: session-cookie-demo
