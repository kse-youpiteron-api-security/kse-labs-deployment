apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: session-cookie-demo-db
  namespace: applications
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: session-cookie-demo-db
  data:
    - secretKey: username
      remoteRef:
        key: secret/data/api-security/session-cookie-demo-db
        property: username
    - secretKey: password
      remoteRef:
        key: secret/data/api-security/session-cookie-demo-db
        property: password
