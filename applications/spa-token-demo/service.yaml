apiVersion: v1
kind: Service
metadata:
  name: spa-token-demo
  namespace: applications
  labels:
    app.kubernetes.io/name: spa-token-demo
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: spa-token-demo
