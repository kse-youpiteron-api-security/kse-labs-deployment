apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: spa-token-demo-db
  namespace: applications
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: spa-token-demo-db
  data:
    - secretKey: username
      remoteRef:
        key: secret/data/api-security/spa-token-demo-db
        property: username
    - secretKey: password
      remoteRef:
        key: secret/data/api-security/spa-token-demo-db
        property: password
